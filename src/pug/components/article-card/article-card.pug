mixin article-card(data)
    a(href='#').article-card&attributes(attributes)
        .image-wrapper 
            img(src=data.img, alt="article")
        .content 
            +badge({title: data.badgeTitle})
            h6 #{data.title}