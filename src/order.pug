extends ./pug/templates/template.pug

block head 
  +head(["header", "mobile-menu", "order-page", "accordion-item", "crumbs", "badge", "popup", "profile", "download-card", "article-card", "selector", "footer"])
block header 
    +header()
    +mobile-menu()
block content
    +crumbs({items: [{title: 'Оформление заказа', href: '#'}]})
    +order-page()
block footer 
    +footer()
    +popup()
    